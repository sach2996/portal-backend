const dbConfig = {
  type: 'mongodb',
  url: 'mongodb+srv://sachin:Sachin@cluster0.3av4b8k.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0',
  synchronize: true, // Set to false in production for better performance
};

export default dbConfig;
